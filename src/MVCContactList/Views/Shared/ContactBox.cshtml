@using MVCContactList.ViewModels
@model ListPersonViewModel

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewBag.Title = "Contact";
}

<tr>
    <td>@Model.Name</td>
    <td>@Model.Email</td>
    <td>
        @if (Model.ShowAsHighlighted)
        {
            <p>VIP!</p>
        }
    </td>
</tr>

@section scripts
{
    <script src=""></script>
}
